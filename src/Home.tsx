import React, { FC, ReactElement } from 'react';
import './Home.scss';

const Home: FC = (): ReactElement => {
	return (
		<h1 className="home titles">WELCOME TO DEC UI, THE BEST UI LIBRARY YOU WILL KNOW TODAY</h1>
	);
};

export default Home;
